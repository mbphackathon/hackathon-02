/*
module Genie.models.project {
    import Pro = Genie.models.pro.Pro;
    import Status = Genie.models.status.Status;
    import Consumer = Genie.models.consumer.Consumer;
    import Category = Genie.models.project.Category;
    export interface Project {
        uuid: string,
        activation: string
        category: Category,
        address: string,
        city: City,
        pro: Pro,
        nb_acceptation: number,
        description: string,
        consumer: Consumer,
        status: Status,
        qualification?: string,
        price?: Price
    }
}*/
